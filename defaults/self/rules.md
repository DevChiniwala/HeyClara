# Rules

Custom instructions and behavioral overrides. Clara reads this file at the start of every session — edits take effect immediately without restart.

Add rules here to change how Clara behaves. Examples:

- "When asked for a standup, keep it to 1-2 lines"
- "Always use bullet points for status updates"
- "Never send messages longer than 3 sentences in Slack channels"
